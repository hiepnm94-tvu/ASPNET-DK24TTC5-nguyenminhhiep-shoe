@using Microsoft.EntityFrameworkCore
@inject quanlybangiay.Data.ApplicationDbContext Db
@{
    var categories = await Db.Categories
        .Where(c => c.IsActive == true)
        .OrderBy(c => c.CategoryName)
        .AsNoTracking()
        .ToListAsync();

    var setting = await Db.Settings.AsNoTracking().FirstOrDefaultAsync();
    var shopName = setting?.ShopName ?? "Shoe Store";

    var cartJson = Context.Session.GetString("ShoppingCart");
    var cartCount = 0;
    if (!string.IsNullOrEmpty(cartJson))
    {
        try
        {
            var items = System.Text.Json.JsonSerializer.Deserialize<List<quanlybangiay.Models.ViewModels.CartItem>>(cartJson);
            cartCount = items?.Sum(i => i.Quantity) ?? 0;
        }
        catch { }
    }
}
<nav class="colorlib-nav" role="navigation">
    <div class="top-menu">
        <div class="container">
            <div class="row">
                <div class="col-sm-7 col-md-9">
                    <div id="colorlib-logo"><a asp-controller="Home" asp-action="Index">@shopName</a></div>
                </div>
                <div class="col-sm-5 col-md-3">
                    <form asp-controller="Product" asp-action="Search" method="get" class="search-wrap">
                        <div class="form-group">
                            <input type="search" name="q" class="form-control search" placeholder="Tìm kiếm sản phẩm...">
                            <button class="btn btn-primary submit-search text-center" type="submit"><i class="icon-search"></i></button>
                        </div>
                    </form>
                </div>
            </div>
            <div class="row">
                <div class="col-sm-12 text-left menu-1">
                    <ul>
                        <li><a asp-controller="Home" asp-action="Index">Trang chủ</a></li>
                        @foreach (var cat in categories)
                        {
                            <li><a asp-controller="Product" asp-action="Category" asp-route-id="@cat.CategoryId">@cat.CategoryName</a></li>
                        }
                        <li><a asp-controller="Contact" asp-action="Index">Liên hệ</a></li>
                        <li class="cart">
                            <a asp-controller="Cart" asp-action="Index">
                                <i class="icon-shopping-cart"></i> Giỏ hàng [<span id="cart-count">@cartCount</span>]
                            </a>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
    <div class="sale">
        <div class="container">
            <div class="row">
                <div class="col-sm-8 offset-sm-2 text-center">
                    <div class="row">
                        <div class="owl-carousel2">
                            <div class="item">
                                <div class="col">
                                    <h3><a href="#">Giảm 25% hầu hết sản phẩm! Mã: SUMMER25</a></h3>
                                </div>
                            </div>
                            <div class="item">
                                <div class="col">
                                    <h3><a href="#">Giảm 50% tất cả giày mùa hè</a></h3>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</nav>
